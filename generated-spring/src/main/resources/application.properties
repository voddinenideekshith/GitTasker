
server.port=8080

# MongoDB Atlas Configuration
# Connection string from MongoDB Atlas - password URL-encoded, database specified in URI
spring.data.mongodb.uri=mongodb+srv://voddinenideekshith_db_user:dv%400905%40DV@gittasker.pxw7weo.mongodb.net/GitTasker?appName=GitTasker
spring.data.mongodb.database=GitTasker

# JPA/Hibernate settings (keeping for potential future use, but not needed for MongoDB)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Use in-memory caching
spring.cache.type=simple

# OAuth2 (GitHub)
# Local testing placeholders - replace these values for local testing only.
# For deployed Container App, set credentials as environment variables
# (do NOT commit secrets). Spring Boot will read environment variables
# automatically when named using uppercase + underscores, e.g.
# SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_GITHUB_CLIENT_ID
# SPRING_SECURITY_OAUTH2_CLIENT_REGISTRATION_GITHUB_CLIENT_SECRET
spring.security.oauth2.client.registration.github.client-id=Ov23li1TRQ8vRiDWuqOL
spring.security.oauth2.client.registration.github.client-secret=f417cf6da331568c01c51f0189495989dbb19027
spring.security.oauth2.client.registration.github.scope=read:user,user:email
spring.security.oauth2.client.provider.github.authorization-uri=https://github.com/login/oauth/authorize
spring.security.oauth2.client.provider.github.token-uri=https://github.com/login/oauth/access_token
spring.security.oauth2.client.provider.github.user-info-uri=https://api.github.com/user

# Dev-friendly Thymeleaf
spring.thymeleaf.cache=false

# MongoDB (Atlas) - optional: set this to use MongoDB. Do NOT commit credentials.
# Example (Atlas):
# spring.data.mongodb.uri=mongodb+srv://<user>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority
# Note: the connection string you pasted appears to include extra '@' characters and looks malformed.
#       Use a valid URI (copy directly from MongoDB Atlas connection string, replacing the user and password placeholders).

