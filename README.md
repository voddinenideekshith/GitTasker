# internship1
A web application to manage tasks with CRUD operations, storing task data as JSON files on IPFS for decentralized and tamper-resistant storage. Features REST API, OAuth2 authentication via GitHub, Redis caching for performance, PostgreSQL for metadata, and a responsive Thymeleaf front-end. 
